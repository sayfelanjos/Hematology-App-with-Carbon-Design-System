apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: supplies-db-pvc
  labels:
    app: supplies-db-pvc
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
