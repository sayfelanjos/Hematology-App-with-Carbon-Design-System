apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: statistics-db-pvc
  labels:
    app: statistics-db-pvc
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
