apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: contracts-db-pvc
  labels:
    app: contracts-db-pvc
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
