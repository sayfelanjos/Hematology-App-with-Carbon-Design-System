# /supplies
post:
  summary: Create a new user.
  requestBody:
    required: true
    content:
      application/json:
  responses:
    '200':
      description: Created
get:
  summary: Returns a user by ID.
  parameters:
    - in: path
      name: userId
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
  responses:
    '200':
      description: OK
      content:
        application/json: