apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: customers-and-suppliers-db-pvc
  labels:
    app: customers-and-suppliers-db-pvc
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
