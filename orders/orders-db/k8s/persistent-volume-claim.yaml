apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: orders-db-pvc
  labels:
    app: orders-db-pvc
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
